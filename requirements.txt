# Pinned for stable, reproducible deploys on Streamlit Cloud.
# Root-cause of prior breakages: the platform silently moving to bleeding-edge Streamlit (1.59,
# broke navigation) and Python (3.14, no wheels for the data stack -> boot loop). Pinning both
# stops the platform from moving under us. Use with runtime.txt -> python-3.11.

streamlit==1.59.1        # the version where navigation now works (with per-page url_path)
pandas==2.2.3
numpy==1.26.4
requests==2.32.3
pytz==2024.2
matplotlib==3.9.2
plotly==5.24.1
pybaseball==2.2.7
psycopg2-binary==2.9.9
