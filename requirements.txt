# Wheel-friendly pins for Streamlit Cloud. IMPORTANT: also set Python to 3.11 in the app's
# Advanced settings (and runtime.txt). On Python 3.14 there are no prebuilt wheels for these,
# so pip tries to COMPILE from source and the boot segfaults. On 3.11 every one installs from
# a prebuilt wheel.
#
# psycopg2-binary and the data stack are left with >= (not ==) so pip can always grab a version
# that HAS a wheel for the running Python, instead of being forced to build one specific version
# from source.

streamlit==1.59.1
pandas>=2.0,<3
numpy>=1.24,<2
requests>=2.31
pytz>=2023.3
matplotlib>=3.7
plotly>=5.18
pybaseball>=2.2
psycopg2-binary>=2.9
